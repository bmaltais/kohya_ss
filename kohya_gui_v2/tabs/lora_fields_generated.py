"""LoRA FieldSpecs -- generated by scripts/gen_lora_fields.py from
wargame/reference/gap-analysis-lora.md + wargame/reference/arch-matrix-lora.md.

Regenerate with: uv run python scripts/gen_lora_fields.py
Hand edits are fine but will be overwritten on regeneration -- move any
permanent hand corrections into the generator or a follow-up patch step.
"""

from ..fields import FieldSpec, Widget


def _to_int(v):
    return int(v) if v not in (None, "") else v


def _to_float(v):
    return float(v) if v not in (None, "") else v


def _to_arg_list(v):
    s = str(v).strip()
    return s.replace('"', "").split() if s else None


def _from_arg_list(v):
    return " ".join(v) if isinstance(v, (list, tuple)) else (v or "")


def _to_int_or_float(v):
    if v in (None, ""):
        return v
    f = float(v)
    return int(f) if f >= 1 else f


LORA_FIELDS = [
    FieldSpec(
        name="adaptive_noise_scale",
        widget=Widget.NUMBER,
        default=0.0,
        label="Adaptive noise scale",
        info="Add `latent mean absolute value * this value` to noise_offset",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="ae",
        widget=Widget.TEXTBOX,
        default="",
        label="VAE Path",
        info="path to ae (*.sft or *.safetensors)",
        archs=frozenset(["flux1", "lumina"]),
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="alpha_mask",
        widget=Widget.CHECKBOX,
        default=False,
        label="Alpha mask",
        info="use alpha channel as mask for training",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="apply_lg_attn_mask",
        widget=Widget.CHECKBOX,
        default=False,
        label="Apply lg attn mask",
        info="apply attention mask (zero embs) to CLIP-L and G",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="apply_t5_attn_mask",
        widget=Widget.CHECKBOX,
        default=False,
        label="Apply T5 Attention Mask",
        info="Apply attention mask to T5-XXL encode and FLUX double blocks",
        archs=frozenset(["flux1", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="async_upload",
        widget=Widget.CHECKBOX,
        default=False,
        label="Async upload",
        info="upload to huggingface asynchronously",
        archs=None,
        training_types=frozenset({"lora"}),
        group="huggingface",
    ),
    FieldSpec(
        name="attn_mode",
        widget=Widget.DROPDOWN,
        default="",
        label="Attention Mode",
        info="xformers requires Split Attention. sageattn does not support training (inference only)",
        archs=frozenset(["anima", "hunyuan_image"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["", "torch", "xformers", "flash", "sageattn", "sdpa"],
    ),
    FieldSpec(
        name="base_weights",
        widget=Widget.TEXTBOX,
        default="",
        label="Base weights",
        info="network weights to merge into the model before training",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
        to_toml=_to_arg_list,
        from_toml=_from_arg_list,
    ),
    FieldSpec(
        name="base_weights_multiplier",
        widget=Widget.TEXTBOX,
        default="",
        label="Base weights multiplier",
        info="multiplier for network weights to merge into the model before training",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
    ),
    FieldSpec(
        name="blocks_to_swap",
        widget=Widget.NUMBER,
        default=0,
        label="Blocks to swap",
        info="The number of blocks to swap. The default is None (no swap). These options must be combined with --fused_backward_pass or --blockwise_fused_optimizers. The recommended maximum value is 36.",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="bucket_no_upscale",
        widget=Widget.CHECKBOX,
        default=False,
        label="Don't upscale bucket resolution",
        info="make bucket for each image without upscaling",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="bucket_reso_steps",
        widget=Widget.NUMBER,
        default=64,
        label="Bucket resolution steps",
        info="steps of resolution for buckets, divisible by 8 is recommended",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="byt5",
        widget=Widget.TEXTBOX,
        default="",
        label="byT5 Path",
        info="path to byt5 (*.sft or *.safetensors), should be float16",
        archs=frozenset(["hunyuan_image"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="cache_info",
        widget=Widget.CHECKBOX,
        default=False,
        label="Cache info",
        info="cache meta information (caption and image size) for faster dataset loading. only available for DreamBooth",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="cache_latents",
        widget=Widget.CHECKBOX,
        default=False,
        label="Cache latents",
        info="cache latents to main memory to reduce VRAM usage (augmentations must be disabled)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
    ),
    FieldSpec(
        name="cache_latents_to_disk",
        widget=Widget.CHECKBOX,
        default=False,
        label="Cache latents to disk",
        info="cache latents to disk to reduce VRAM usage (augmentations must be disabled)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
    ),
    FieldSpec(
        name="cache_text_encoder_outputs",
        widget=Widget.CHECKBOX,
        default=False,
        label="Cache Text Encoder Outputs",
        info="Cache Qwen3 text encoder outputs to reduce VRAM usage. Recommended when not training Text Encoder LoRA.",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3", "sdxl"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="cache_text_encoder_outputs_to_disk",
        widget=Widget.CHECKBOX,
        default=False,
        label="Cache Text Encoder Outputs to Disk",
        info="Cache text encoder outputs to disk. Automatically enables Cache Text Encoder Outputs.",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3", "sdxl"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="caption_dropout_every_n_epochs",
        widget=Widget.NUMBER,
        default=0,
        label="Dropout caption every n epochs",
        info="Dropout all captions every N epochs",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="caption_dropout_rate",
        widget=Widget.NUMBER,
        default=0.0,
        label="Rate of caption dropout",
        info="Rate out dropout caption(0.0~1.0)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="caption_extension",
        widget=Widget.DROPDOWN,
        default=".caption",
        label="Caption file extension",
        info="extension of caption files",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        choices=["", ".cap", ".caption", ".txt"],
    ),
    FieldSpec(
        name="caption_prefix",
        widget=Widget.TEXTBOX,
        default="",
        label="Caption prefix",
        info="prefix for caption text",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
    ),
    FieldSpec(
        name="caption_separator",
        widget=Widget.TEXTBOX,
        default=",",
        label="Caption separator",
        info="separator for caption",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
    ),
    FieldSpec(
        name="caption_suffix",
        widget=Widget.TEXTBOX,
        default="",
        label="Caption suffix",
        info="suffix for caption text",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
    ),
    FieldSpec(
        name="caption_tag_dropout_rate",
        widget=Widget.NUMBER,
        default=0.0,
        label="Caption tag dropout rate",
        info="Rate out dropout comma separated tokens(0.0~1.0)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="clip_g",
        widget=Widget.TEXTBOX,
        default="",
        label="CLIP-G Path",
        info="CLIP-G model path. if not specified, use ckpt's state_dict",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="clip_g_dropout_rate",
        widget=Widget.NUMBER,
        default=0.0,
        label="CLIP-G Dropout Rate",
        info="Dropout rate for CLIP-G encoder",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="clip_l",
        widget=Widget.TEXTBOX,
        default="",
        label="CLIP-L Path",
        info="path to clip_l (*.sft or *.safetensors), should be float16",
        archs=frozenset(["flux1", "sd3"]),
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="clip_l_dropout_rate",
        widget=Widget.NUMBER,
        default=0.0,
        label="CLIP-L Dropout Rate",
        info="Dropout rate for CLIP-L encoder",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="clip_skip",
        widget=Widget.NUMBER,
        default=0,
        label="Clip skip",
        info="use output of nth layer from back of text encoder (n>=1)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="color_aug",
        widget=Widget.CHECKBOX,
        default=False,
        label="Color augmentation",
        info="Enable weak color augmentation",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="compile",
        widget=Widget.CHECKBOX,
        default=False,
        label="Per-block Torch Compile",
        info="Enable per-block torch.compile for the DiT (requires Triton). ~20% speedup. Mutually exclusive with Legacy Torch Compile below.",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="compile_backend",
        widget=Widget.TEXTBOX,
        default="inductor",
        label="Compile Backend",
        info="torch.compile backend for Per-block Torch Compile",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="compile_cache_size_limit",
        widget=Widget.NUMBER,
        default=0,
        label="Compile Cache Size Limit",
        info="torch._dynamo.config.cache_size_limit for Per-block Torch Compile. 0 uses the PyTorch default; 32 is recommended for multi-resolution datasets.",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="compile_dynamic",
        widget=Widget.DROPDOWN,
        default="",
        label="Compile Dynamic Shapes",
        info="Dynamic shapes mode for Per-block Torch Compile. On Windows, 'true' requires the Visual Studio 2022 C++ compiler.",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["", "true", "false", "auto"],
    ),
    FieldSpec(
        name="compile_fullgraph",
        widget=Widget.CHECKBOX,
        default=False,
        label="Compile Fullgraph",
        info="Enable fullgraph mode for Per-block Torch Compile. Cannot be used with Split Attention.",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="compile_mode",
        widget=Widget.DROPDOWN,
        default="default",
        label="Compile Mode",
        info="torch.compile mode for Per-block Torch Compile (default recommended for training)",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=[
            "default",
            "reduce-overhead",
            "max-autotune",
            "max-autotune-no-cudagraphs",
        ],
    ),
    FieldSpec(
        name="conditioning_data_dir",
        widget=Widget.TEXTBOX,
        default="",
        label="Conditioning data dir",
        info="conditioning data directory",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="console_log_file",
        widget=Widget.TEXTBOX,
        default="",
        label="Console log file",
        info="Log to a file instead of stderr",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="console_log_level",
        widget=Widget.DROPDOWN,
        default="",
        label="Console log level",
        info="Set the logging level, default is INFO",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["", "DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"],
    ),
    FieldSpec(
        name="console_log_simple",
        widget=Widget.CHECKBOX,
        default=False,
        label="Console log simple",
        info="Simple log output",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="controlnet_model_name_or_path",
        widget=Widget.TEXTBOX,
        default="",
        label="Controlnet model name or path",
        info="path to controlnet (*.sft or *.safetensors)",
        archs=frozenset(["flux1"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="cpu_offload_checkpointing",
        widget=Widget.CHECKBOX,
        default=False,
        label="CPU Offload Checkpointing",
        info="[Experimental] Enable offloading of tensors to CPU during checkpointing",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="cross_attn_lr",
        widget=Widget.NUMBER,
        default=0.0,
        label="Cross attn lr",
        info="Learning rate for cross-attention layers. None=same as base LR, 0=freeze",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="cuda_allow_tf32",
        widget=Widget.CHECKBOX,
        default=False,
        label="Cuda allow tf32",
        info="Allow TF32 precision on Ampere or newer GPUs (improves performance)",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="cuda_cudnn_benchmark",
        widget=Widget.CHECKBOX,
        default=False,
        label="Cuda cudnn benchmark",
        info="Enable cuDNN benchmark mode (may improve performance)",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="dataset_class",
        widget=Widget.TEXTBOX,
        default="",
        label="Dataset class",
        info="dataset class for arbitrary dataset (package.module.Class)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
    ),
    FieldSpec(
        name="dataset_config",
        widget=Widget.TEXTBOX,
        default="",
        label="Dataset config file (Optional. Select the toml configuration file to use for the dataset)",
        info="Multi-resolution / duplicate-subset datasets are configured entirely in this TOML file, not via GUI fields. See 'Behavior when there are duplicate subsets': https://github.com/kohya-ss/sd-scripts/blob/main/docs/config_README-en.md#behavior-when-there-are-duplicate-subsets",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="dataset_repeats",
        widget=Widget.NUMBER,
        default=1,
        label="Dataset repeats",
        info="repeat dataset when training with captions",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="debiased_estimation_loss",
        widget=Widget.CHECKBOX,
        default=False,
        label="Debiased Estimation loss",
        info="Automates the processing of noise, allowing for faster model fitting, as well as balancing out color issues. Do not use if Min SNR gamma is specified.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="dim_from_weights",
        widget=Widget.CHECKBOX,
        default=False,
        label="Dim from weights",
        info="automatically determine dim (rank) from network_weights",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
    ),
    FieldSpec(
        name="disable_mmap_load_safetensors",
        widget=Widget.CHECKBOX,
        default=False,
        label="Disable mmap load safe tensors",
        info="Disable memory mapping when loading the model's .safetensors in SDXL.",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3", "sdxl"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="discrete_flow_shift",
        widget=Widget.NUMBER,
        default=3.0,
        label="Discrete Flow Shift",
        info="Timestep distribution shift for rectified flow training, used when Timestep Sampling is 'shift'. Default is 1.0",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="dynamo_backend",
        widget=Widget.DROPDOWN,
        default="no",
        label="Dynamo backend",
        info="The backend to use for the dynamo JIT compiler.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="accelerate_launch",
        choices=[
            "no",
            "eager",
            "aot_eager",
            "inductor",
            "aot_ts_nvfuser",
            "nvprims_nvfuser",
            "cudagraphs",
            "ofi",
            "fx2trt",
            "onnxrt",
            "tensorrt",
            "ipex",
            "tvm",
        ],
    ),
    FieldSpec(
        name="enable_bucket",
        widget=Widget.CHECKBOX,
        default=False,
        label="Enable buckets",
        info="enable buckets for multi aspect ratio training",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
    ),
    FieldSpec(
        name="enable_scaled_pos_embed",
        widget=Widget.CHECKBOX,
        default=False,
        label="Enable Scaled Positional Embeddings",
        info="Enable scaled positional embeddings in the model.",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="enable_wildcard",
        widget=Widget.CHECKBOX,
        default=False,
        label="Enable wildcard",
        info="enable wildcard for caption (e.g. '{image|picture|rendition}')",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="face_crop_aug_range",
        widget=Widget.TEXTBOX,
        default="",
        label="Face crop aug range",
        info="enable face-centered crop augmentation and its range (e.g. 2.0,4.0)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
    ),
    FieldSpec(
        name="flip_aug",
        widget=Widget.CHECKBOX,
        default=False,
        label="Flip augmentation",
        info="Enable horizontal flip augmentation",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="fp8_base",
        widget=Widget.CHECKBOX,
        default=False,
        label="fp8 base",
        info="Use fp8 for base model",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="fp8_base_unet",
        widget=Widget.CHECKBOX,
        default=False,
        label="fp8 base unet",
        info="Flux can be trained with fp8, and CLIP-L can be trained with bf16/fp16.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="fp8_scaled",
        widget=Widget.CHECKBOX,
        default=False,
        label="FP8 Scaled",
        info="Use scaled fp8 for the DiT model to reduce VRAM usage",
        archs=frozenset(["hunyuan_image"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="fp8_vl",
        widget=Widget.CHECKBOX,
        default=False,
        label="FP8 VLM",
        info="Use fp8 for the Qwen2.5-VL text encoder",
        archs=frozenset(["hunyuan_image"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="full_bf16",
        widget=Widget.CHECKBOX,
        default=False,
        label="Full bf16 training (experimental)",
        info="Required bitsandbytes >= 0.36.0",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="full_fp16",
        widget=Widget.CHECKBOX,
        default=False,
        label="Full fp16 training (experimental)",
        info="fp16 training including gradients, some models are not supported",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="fused_backward_pass",
        widget=Widget.CHECKBOX,
        default=False,
        label="Fused backward pass",
        info="Enable fused backward pass. This option is useful to reduce the GPU memory usage. Can't be used if Fused optimizer groups is > 0. Only AdaFactor is supported",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="gemma2",
        widget=Widget.TEXTBOX,
        default="",
        label="Gemma2 Path",
        info="path to gemma2 model (*.sft or *.safetensors), should be float16",
        archs=frozenset(["lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="gemma2_max_token_length",
        widget=Widget.NUMBER,
        default=0,
        label="Gemma2 Max Token Length",
        info="Maximum token length for Gemma2. Backend default is 256 when omitted.",
        archs=frozenset(["lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="gradient_accumulation_steps",
        widget=Widget.NUMBER,
        default=1,
        label="Gradient accumulation steps",
        info="Number of updates steps to accumulate before performing a backward/update pass",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="gradient_checkpointing",
        widget=Widget.CHECKBOX,
        default=False,
        label="Gradient checkpointing",
        info="enable gradient checkpointing",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
    ),
    FieldSpec(
        name="guidance_scale",
        widget=Widget.NUMBER,
        default=3.5,
        label="Guidance Scale",
        info="Guidance scale for Flux1",
        archs=frozenset(["flux1"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="highvram",
        widget=Widget.CHECKBOX,
        default=False,
        label="highvram",
        info="Disable low VRAM optimization. e.g. do not clear CUDA cache after each latent caching (for machines which have bigger VRAM)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="huber_c",
        widget=Widget.NUMBER,
        default=0.1,
        label="Huber C",
        info="The huber loss parameter. Only used if one of the huber loss modes (huber or smooth l1) is selected with loss_type",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="huber_scale",
        widget=Widget.NUMBER,
        default=1.0,
        label="Huber scale",
        info="The Huber loss scale parameter. Only used if one of the huber loss modes (huber or smooth l1) is selected with loss_type.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="huber_schedule",
        widget=Widget.DROPDOWN,
        default="snr",
        label="Huber schedule",
        info="The type of loss to use and whether it's scheduled based on the timestep",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["constant", "exponential", "snr"],
    ),
    FieldSpec(
        name="huggingface_path_in_repo",
        widget=Widget.TEXTBOX,
        default="",
        label="Huggingface path in repo",
        info="huggingface model path to upload files",
        archs=None,
        training_types=frozenset({"lora"}),
        group="huggingface",
    ),
    FieldSpec(
        name="huggingface_repo_id",
        widget=Widget.TEXTBOX,
        default="",
        label="HuggingFace repo id",
        info="huggingface repo name to upload",
        archs=None,
        training_types=frozenset({"lora"}),
        group="huggingface",
    ),
    FieldSpec(
        name="huggingface_repo_type",
        widget=Widget.TEXTBOX,
        default="",
        label="Huggingface repo type",
        info="huggingface repo type to upload",
        archs=None,
        training_types=frozenset({"lora"}),
        group="huggingface",
    ),
    FieldSpec(
        name="huggingface_repo_visibility",
        widget=Widget.TEXTBOX,
        default="",
        label="Huggingface repo visibility",
        info="huggingface repository visibility ('public' for public, 'private' or None for private)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="huggingface",
    ),
    FieldSpec(
        name="huggingface_token",
        widget=Widget.TEXTBOX,
        default="",
        label="Huggingface token",
        info="huggingface token",
        archs=None,
        training_types=frozenset({"lora"}),
        group="huggingface",
    ),
    FieldSpec(
        name="initial_epoch",
        widget=Widget.NUMBER,
        default=0,
        label="Initial epoch",
        info="initial epoch number, 1 means first epoch (same as not specifying). NOTE: initial_epoch/step doesn't affect to lr scheduler. Which means lr scheduler will start from 0 without `--resume`.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="initial_step",
        widget=Widget.NUMBER,
        default=0,
        label="Initial step",
        info="initial step number including all epochs, 0 means first step (same as not specifying). overwrites initial_epoch.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="ip_noise_gamma",
        widget=Widget.NUMBER,
        default=0.0,
        label="IP noise gamma",
        info="enable input perturbation noise. used for regularization. recommended value: around 0.1",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="ip_noise_gamma_random_strength",
        widget=Widget.CHECKBOX,
        default=False,
        label="IP noise gamma random strength",
        info="Use random strength between 0~ip_noise_gamma for input perturbation noise",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="keep_tokens",
        widget=Widget.NUMBER,
        default=0,
        label="Keep n tokens",
        info="keep heading N tokens when shuffling caption tokens (token means comma separated strings)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="keep_tokens_separator",
        widget=Widget.TEXTBOX,
        default="",
        label="Keep tokens separator",
        info="A custom separator to divide the caption into fixed and flexible parts. Tokens before this separator will not be shuffled. If not specified, '--keep_tokens' will be used to determine the fixed number of tokens.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
    ),
    FieldSpec(
        name="learning_rate",
        widget=Widget.NUMBER,
        default=2e-06,
        label="Learning rate",
        info="Set to 0 to not train the Unet",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="llm_adapter_lr",
        widget=Widget.NUMBER,
        default=0.0,
        label="Llm adapter lr",
        info="Learning rate for LLM adapter. None=same as base LR, 0=freeze adapter",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="llm_adapter_path",
        widget=Widget.TEXTBOX,
        default="",
        label="LLM Adapter Path",
        info="If omitted, the adapter is loaded from the DiT file when the key llm_adapter.out_proj.weight exists",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="log_config",
        widget=Widget.CHECKBOX,
        default=False,
        label="Log config",
        info="Log training parameter to WANDB",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="log_prefix",
        widget=Widget.TEXTBOX,
        default="",
        label="Log prefix",
        info="add prefix for each log directory",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="log_tracker_config",
        widget=Widget.TEXTBOX,
        default="",
        label="Log tracker config",
        info="Path to tracker config file to use for logging",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="log_tracker_name",
        widget=Widget.TEXTBOX,
        default="",
        label="Log tracker name",
        info="Name of tracker to use for logging, default is script-specific default name",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="log_with",
        widget=Widget.DROPDOWN,
        default="",
        label="Logging",
        info="Loggers to use, tensorboard will be used as the default.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["", "tensorboard", "wandb", "all"],
    ),
    FieldSpec(
        name="logging_dir",
        widget=Widget.TEXTBOX,
        default="",
        label="Logging directory (Optional. to enable logging and output Tensorboard log)",
        info="enable logging and output TensorBoard log to this directory",
        archs=None,
        training_types=frozenset({"lora"}),
        group="folders",
    ),
    FieldSpec(
        name="logit_mean",
        widget=Widget.NUMBER,
        default=0.0,
        label="Logit Mean",
        info="mean to use when using the `'logit_normal'` weighting scheme",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="logit_std",
        widget=Widget.NUMBER,
        default=1.0,
        label="Logit Std",
        info="std to use when using the `'logit_normal'` weighting scheme",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="loss_type",
        widget=Widget.DROPDOWN,
        default="l2",
        label="Loss type",
        info="The type of loss to use and whether it's scheduled based on the timestep",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["l1", "l2", "huber", "smooth_l1"],
    ),
    FieldSpec(
        name="lowram",
        widget=Widget.CHECKBOX,
        default=False,
        label="lowvram",
        info="Enable low RAM optimization. e.g. load models to VRAM instead of RAM (for machines which have bigger VRAM than RAM such as Colab and Kaggle)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="lr_decay_steps",
        widget=Widget.TEXTBOX,
        default=0,
        label="Lr decay steps",
        info="Int number of steps for the decay in the lr scheduler (default is 0) or float (<1) with ratio of train steps",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int_or_float,
        from_toml=_to_int_or_float,
    ),
    FieldSpec(
        name="lr_scheduler",
        widget=Widget.DROPDOWN,
        default="constant",
        label="LR Scheduler",
        info="scheduler to use for learning rate",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        choices=[
            "adafactor",
            "constant",
            "constant_with_warmup",
            "cosine",
            "cosine_with_restarts",
            "linear",
            "piecewise_constant",
            "polynomial",
            "cosine_with_min_lr",
            "inverse_sqrt",
            "warmup_stable_decay",
        ],
    ),
    FieldSpec(
        name="lr_scheduler_args",
        widget=Widget.TEXTBOX,
        default="",
        label="LR scheduler extra arguments",
        info='additional arguments for scheduler (like "T_max=100")',
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_arg_list,
        from_toml=_from_arg_list,
    ),
    FieldSpec(
        name="lr_scheduler_min_lr_ratio",
        widget=Widget.NUMBER,
        default=0.0,
        label="Lr scheduler min lr ratio",
        info="The minimum learning rate as a ratio of the initial learning rate for cosine with min lr scheduler and warmup decay scheduler",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="lr_scheduler_num_cycles",
        widget=Widget.NUMBER,
        default=1,
        label="LR scheduler num cycles",
        info="Number of restarts for cosine scheduler with restarts",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="lr_scheduler_power",
        widget=Widget.NUMBER,
        default=1,
        label="LR scheduler power",
        info="Polynomial power for polynomial scheduler",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="lr_scheduler_timescale",
        widget=Widget.NUMBER,
        default=0,
        label="Lr scheduler timescale",
        info="Inverse sqrt timescale for inverse sqrt scheduler,defaults to `num_warmup_steps`",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="lr_scheduler_type",
        widget=Widget.DROPDOWN,
        default="",
        label="LR Scheduler type",
        info="(Optional) custom scheduler module name",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        choices=["", "CosineAnnealingLR"],
    ),
    FieldSpec(
        name="lr_warmup_steps",
        widget=Widget.TEXTBOX,
        default=0,
        label="LR warmup steps",
        info="Int number of steps for the warmup in the lr scheduler (default is 0) or float with ratio of train steps",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int_or_float,
        from_toml=_to_int_or_float,
    ),
    FieldSpec(
        name="masked_loss",
        widget=Widget.CHECKBOX,
        default=False,
        label="Masked loss",
        info="Apply mask for calculating loss. conditioning_data_dir is required for dataset",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="max_bucket_reso",
        widget=Widget.NUMBER,
        default=1024,
        label="Max bucket resolution",
        info="Maximum size in pixel a bucket can be (>= 64)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="max_data_loader_n_workers",
        widget=Widget.NUMBER,
        default=8,
        label="Max num workers for DataLoader",
        info="Override number of epoch. Default: 0",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="max_grad_norm",
        widget=Widget.NUMBER,
        default=1.0,
        label="Max grad norm",
        info="Max gradient norm, 0 for no clipping",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="max_timestep",
        widget=Widget.NUMBER,
        default=0,
        label="Max Timestep",
        info="Values lower than 1000 will make the model more img2img focussed. 1000 = noise only",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="max_token_length",
        widget=Widget.DROPDOWN,
        default=75,
        label="Max Token Length",
        info="max token length of text encoder",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
        choices=[75, 150, 225],
    ),
    FieldSpec(
        name="max_train_epochs",
        widget=Widget.NUMBER,
        default=0,
        label="Max train epochs",
        info="training epochs (overrides max_train_steps). 0 = no override",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="max_train_steps",
        widget=Widget.NUMBER,
        default=1600,
        label="Max train steps",
        info="Overrides # training steps. 0 = no override",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="max_validation_steps",
        widget=Widget.NUMBER,
        default=0,
        label="Max validation steps",
        info="Max number of validation dataset items processed. By default, validation will run the entire validation dataset",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="mem_eff_attn",
        widget=Widget.CHECKBOX,
        default=False,
        label="Memory efficient attention",
        info="use memory efficient attention for CrossAttention",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="metadata_author",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata author",
        info="author name for model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_description",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata description",
        info="description for model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_is_negative_embedding",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata is negative embedding",
        info="whether this is a negative embedding for model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_license",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata license",
        info="license for model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_merged_from",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata merged from",
        info="source models for merged model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_preprocessor",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata preprocessor",
        info="preprocessor used for model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_tags",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata tags",
        info="tags for model metadata, separated by comma",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_thumbnail",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata thumbnail",
        info="thumbnail image as data URL or file path (will be converted to data URL) for model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_title",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata title",
        info="title for model metadata (default is output_name)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_trigger_phrase",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata trigger phrase",
        info="trigger phrase for model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="metadata_usage_hint",
        widget=Widget.TEXTBOX,
        default="",
        label="Metadata usage hint",
        info="usage hint for model metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="metadata",
    ),
    FieldSpec(
        name="min_bucket_reso",
        widget=Widget.NUMBER,
        default=256,
        label="Min bucket resolution",
        info="Minimum size in pixel a bucket can be (>= 64)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="min_snr_gamma",
        widget=Widget.NUMBER,
        default=0.0,
        label="Min SNR gamma",
        info="Recommended value of 5 when used",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="min_timestep",
        widget=Widget.NUMBER,
        default=0,
        label="Min Timestep",
        info="Values greater than 0 will make the model more img2img focussed. 0 = image only",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="mixed_precision",
        widget=Widget.DROPDOWN,
        default="no",
        label="Mixed precision",
        info="Whether or not to use mixed precision training.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="accelerate_launch",
        choices=["no", "fp16", "bf16"],
    ),
    FieldSpec(
        name="mlp_lr",
        widget=Widget.NUMBER,
        default=0.0,
        label="Mlp lr",
        info="Learning rate for MLP layers. None=same as base LR, 0=freeze",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="mod_lr",
        widget=Widget.NUMBER,
        default=0.0,
        label="Mod lr",
        info="Learning rate for AdaLN modulation layers. None=same as base LR, 0=freeze. Note: mod layers are not included in LoRA by default.",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="mode_scale",
        widget=Widget.NUMBER,
        default=1.29,
        label="Mode Scale",
        info="Scale of mode weighting scheme. Only effective when using the `'mode'` as the `weighting_scheme`",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="model_prediction_type",
        widget=Widget.DROPDOWN,
        default="sigma_scaled",
        label="Model Prediction Type",
        info="How to interpret and process the model prediction: raw (use as is), additive (add to noisy input), sigma_scaled (apply sigma scaling).",
        archs=frozenset(["flux1", "hunyuan_image", "lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["raw", "additive", "sigma_scaled"],
    ),
    FieldSpec(
        name="model_type",
        widget=Widget.DROPDOWN,
        default="flux",
        label="Model type",
        info="Model type to use for training",
        archs=frozenset(["flux1"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["flux", "chroma"],
    ),
    FieldSpec(
        name="multires_noise_discount",
        widget=Widget.NUMBER,
        default=0.3,
        label="Multires noise discount",
        info="Recommended values are 0.8. For LoRAs with small datasets, 0.1-0.3",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="multires_noise_iterations",
        widget=Widget.NUMBER,
        default=0,
        label="Multires noise iterations",
        info="Enable multires noise (recommended values are 6-10)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="network_alpha",
        widget=Widget.NUMBER,
        default=1,
        label="Network Alpha",
        info="alpha for LoRA weight scaling, default 1 (same as network_dim for same behavior as old version)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="network_args",
        widget=Widget.TEXTBOX,
        default="",
        label="Network args",
        info="additional arguments for network (key=value)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
        to_toml=_to_arg_list,
        from_toml=_from_arg_list,
    ),
    FieldSpec(
        name="network_dim",
        widget=Widget.NUMBER,
        default=0,
        label="Network Rank (Dimension)",
        info="network dimensions (depends on each network)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="network_dropout",
        widget=Widget.NUMBER,
        default=0.0,
        label="Network dropout",
        info="Drops neurons out of training every step (0 or None is default behavior (no dropout), 1 would drop all neurons)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="network_module",
        widget=Widget.TEXTBOX,
        default="",
        label="Network module",
        info="network module to train",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
    ),
    FieldSpec(
        name="network_train_text_encoder_only",
        widget=Widget.CHECKBOX,
        default=False,
        label="Train text encoder only",
        info="only training Text Encoder part",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
    ),
    FieldSpec(
        name="network_train_unet_only",
        widget=Widget.CHECKBOX,
        default=False,
        label="Train U-Net only",
        info="only training U-Net part",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
    ),
    FieldSpec(
        name="network_weights",
        widget=Widget.TEXTBOX,
        default="",
        label="Network weights",
        info="pretrained weights for network",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
    ),
    FieldSpec(
        name="no_half_vae",
        widget=Widget.CHECKBOX,
        default=False,
        label="No half vae",
        info="do not use fp16/bf16 VAE in mixed precision (use float VAE)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="no_metadata",
        widget=Widget.CHECKBOX,
        default=False,
        label="No metadata",
        info="do not save metadata in output model",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="noise_offset",
        widget=Widget.NUMBER,
        default=0.0,
        label="Noise offset",
        info="Recommended values are 0.05 - 0.15",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="noise_offset_random_strength",
        widget=Widget.CHECKBOX,
        default=False,
        label="Noise offset random strength",
        info="Use random strength between 0~noise_offset for noise offset",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="optimizer_args",
        widget=Widget.TEXTBOX,
        default="",
        label="Optimizer extra arguments",
        info='additional arguments for optimizer (like "weight_decay=0.01 betas=0.9,0.999 ...")',
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_arg_list,
        from_toml=_from_arg_list,
    ),
    FieldSpec(
        name="optimizer_type",
        widget=Widget.DROPDOWN,
        default="",
        label="Optimizer",
        info="Optimizer to use",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        choices=[
            "",
            "AdamW",
            "AdamWScheduleFree",
            "AdamW8bit",
            "Adafactor",
            "bitsandbytes.optim.AdEMAMix8bit",
            "bitsandbytes.optim.PagedAdEMAMix8bit",
            "DAdaptation",
            "DAdaptAdaGrad",
            "DAdaptAdam",
            "DAdaptAdan",
            "DAdaptAdanIP",
            "DAdaptAdamPreprint",
            "DAdaptLion",
            "DAdaptSGD",
            "Lion",
            "Lion8bit",
            "PagedAdamW8bit",
            "PagedAdamW32bit",
            "PagedLion8bit",
            "Prodigy",
            "prodigyplus.ProdigyPlusScheduleFree",
            "pytorch_optimizer.CAME",
            "RAdamScheduleFree",
            "SGDNesterov",
            "SGDNesterov8bit",
            "SGDScheduleFree",
        ],
    ),
    FieldSpec(
        name="output_dir",
        widget=Widget.TEXTBOX,
        default="",
        label="Output directory for trained model",
        info="directory to output trained model",
        archs=None,
        training_types=frozenset({"lora"}),
        group="folders",
    ),
    FieldSpec(
        name="output_name",
        widget=Widget.TEXTBOX,
        default="",
        label="Trained Model output name",
        info="base name of trained model file",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="persistent_data_loader_workers",
        widget=Widget.CHECKBOX,
        default=False,
        label="Persistent data loader",
        info="persistent DataLoader workers (useful for reduce time gap between epoch, but may use more memory)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="pos_emb_random_crop_rate",
        widget=Widget.NUMBER,
        default=0.0,
        label="Positional Embedding Random Crop Rate",
        info="Random crop rate for positional embeddings",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="pretrained_model_name_or_path",
        widget=Widget.TEXTBOX,
        default="",
        label="Pretrained model name or path",
        info="pretrained model to train, directory to Diffusers model or StableDiffusion checkpoint",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="prior_loss_weight",
        widget=Widget.NUMBER,
        default=1.0,
        label="Prior loss weight",
        info="loss weight for regularization images",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="qwen3",
        widget=Widget.TEXTBOX,
        default="",
        label="Qwen3 Path",
        info="Path to Qwen3-0.6B model (safetensors file or directory)",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="qwen3_max_token_length",
        widget=Widget.NUMBER,
        default=512,
        label="Qwen3 Max Token Length",
        info="Maximum token length for the Qwen3 tokenizer",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="qwen_image_vae_2d",
        widget=Widget.CHECKBOX,
        default=False,
        label="Qwen-Image VAE 2D",
        info="2D Qwen-Image VAE: ~2x faster encode/decode, ~1/3 peak VRAM for single-image caching. VAE Disable Cache has no effect here (2D VAE has no temporal cache).",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="random_crop",
        widget=Widget.CHECKBOX,
        default=False,
        label="Random crop instead of center crop",
        info="enable random crop (for style training in face-centered crop augmentation)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="reg_data_dir",
        widget=Widget.TEXTBOX,
        default="",
        label="Reg data dir",
        info="directory for regularization images",
        archs=None,
        training_types=frozenset({"lora"}),
        group="folders",
    ),
    FieldSpec(
        name="resize_interpolation",
        widget=Widget.DROPDOWN,
        default="",
        label="Resize interpolation",
        info="Resize interpolation when required. Default: area Options: lanczos, nearest, bilinear, bicubic, area",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=[
            "",
            "lanczos",
            "nearest",
            "bilinear",
            "linear",
            "bicubic",
            "cubic",
            "area",
        ],
    ),
    FieldSpec(
        name="resolution",
        widget=Widget.TEXTBOX,
        default="",
        label="Resolution",
        info="resolution in training ('size' or 'width,height')",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
    ),
    FieldSpec(
        name="resume",
        widget=Widget.TEXTBOX,
        default="",
        label='Resume from saved training state (path to "last-state" state folder)',
        info="Saved state to resume training from",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="resume_from_huggingface",
        widget=Widget.CHECKBOX,
        default=False,
        label="Resume from huggingface",
        info="resume from huggingface (ex: --resume {repo_id}/{path_in_repo}:{revision}:{repo_type})",
        archs=None,
        training_types=frozenset({"lora"}),
        group="huggingface",
    ),
    FieldSpec(
        name="sample_at_first",
        widget=Widget.CHECKBOX,
        default=False,
        label="Sample at first",
        info="generate sample images before training",
        archs=None,
        training_types=frozenset({"lora"}),
        group="samples",
    ),
    FieldSpec(
        name="sample_batch_size",
        widget=Widget.NUMBER,
        default=0,
        label="Sample batch size",
        info="Batch size to use for sampling, defaults to --training_batch_size value. Sample batches are bucketed by width, height, guidance scale, and seed",
        archs=frozenset(["lumina"]),
        training_types=frozenset({"lora"}),
        group="samples",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="sample_every_n_epochs",
        widget=Widget.NUMBER,
        default=0,
        label="Sample every n epochs",
        info="generate sample images every N epochs (overwrites n_steps)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="samples",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="sample_every_n_steps",
        widget=Widget.NUMBER,
        default=0,
        label="Sample every n steps",
        info="generate sample images every N steps",
        archs=None,
        training_types=frozenset({"lora"}),
        group="samples",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="sample_prompts",
        widget=Widget.TEXTBOX,
        default="",
        label="Sample prompts",
        info="One prompt per line. Flags: --w width, --h height, --d seed, --l cfg scale, --s steps, --n negative. Edit prompt.txt in samples dir to update live.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="samples",
    ),
    FieldSpec(
        name="sample_sampler",
        widget=Widget.DROPDOWN,
        default="ddim",
        label="Sample sampler",
        info="sampler (scheduler) type for sample images",
        archs=None,
        training_types=frozenset({"lora"}),
        group="samples",
        choices=[
            "ddim",
            "pndm",
            "lms",
            "euler",
            "euler_a",
            "heun",
            "dpm_2",
            "dpm_2_a",
            "dpmsolver",
            "dpmsolver++",
            "dpmsingle",
            "k_lms",
            "k_euler",
            "k_euler_a",
            "k_dpm_2",
            "k_dpm_2_a",
        ],
    ),
    FieldSpec(
        name="save_clip",
        widget=Widget.CHECKBOX,
        default=False,
        label="Save CLIP models",
        info="[DOES NOT WORK] unified checkpoint is not supported",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="save_every_n_epochs",
        widget=Widget.NUMBER,
        default=0,
        label="Save every N epochs",
        info="save checkpoint every N epochs",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="save_every_n_steps",
        widget=Widget.NUMBER,
        default=0,
        label="Save every N steps",
        info="(Optional) The model is saved every specified steps",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="save_last_n_epochs",
        widget=Widget.NUMBER,
        default=0,
        label="Save last N epochs",
        info="(Optional) Save only the specified number of epochs (old epochs will be deleted)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="save_last_n_epochs_state",
        widget=Widget.NUMBER,
        default=0,
        label="Save last N epochs state",
        info="(Optional) Save only the specified number of epochs states (old models will be deleted)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="save_last_n_steps",
        widget=Widget.NUMBER,
        default=0,
        label="Save last N steps",
        info="(Optional) Save only the specified number of models (old models will be deleted)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="save_last_n_steps_state",
        widget=Widget.NUMBER,
        default=0,
        label="Save last N steps state",
        info="(Optional) Save only the specified number of states (old models will be deleted)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="save_model_as",
        widget=Widget.DROPDOWN,
        default="safetensors",
        label="Save trained model as",
        info="format to save the model (default is .safetensors)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
        choices=["", "ckpt", "pt", "safetensors"],
    ),
    FieldSpec(
        name="save_n_epoch_ratio",
        widget=Widget.NUMBER,
        default=0,
        label="Save n epoch ratio",
        info="save checkpoint N epoch ratio (for example 5 means save at least 5 files total)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="save_precision",
        widget=Widget.DROPDOWN,
        default="",
        label="Save precision",
        info="precision in saving",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
        choices=["", "float", "fp16", "bf16"],
    ),
    FieldSpec(
        name="save_state",
        widget=Widget.CHECKBOX,
        default=False,
        label="Save training state",
        info="Save training state (including optimizer states etc.) when saving models",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="save_state_on_train_end",
        widget=Widget.CHECKBOX,
        default=False,
        label="Save training state at end of training",
        info="Save training state (including optimizer states etc.) on train end",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="save_state_to_huggingface",
        widget=Widget.CHECKBOX,
        default=False,
        label="Save state to huggingface",
        info="save state to huggingface",
        archs=None,
        training_types=frozenset({"lora"}),
        group="huggingface",
    ),
    FieldSpec(
        name="save_t5xxl",
        widget=Widget.CHECKBOX,
        default=False,
        label="Save T5-XXL model",
        info="[DOES NOT WORK] unified checkpoint is not supported",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="scale_v_pred_loss_like_noise_pred",
        widget=Widget.CHECKBOX,
        default=False,
        label="Scale v prediction loss",
        info="SD v2 only. Scales loss by timestep so global/local noise prediction weights match; may improve detail.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="scale_weight_norms",
        widget=Widget.NUMBER,
        default=0.0,
        label="Scale weight norms",
        info="Scale the weight of each key pair to help prevent overtraing via exploding gradients. (1 is a good starting point)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="network",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="sdpa",
        widget=Widget.CHECKBOX,
        default=False,
        label="Sdpa",
        info="use sdpa for CrossAttention (requires PyTorch 2.0)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="secondary_separator",
        widget=Widget.TEXTBOX,
        default="",
        label="Secondary separator",
        info="a secondary separator for caption. This separator is replaced to caption_separator after dropping/shuffling caption",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="seed",
        widget=Widget.NUMBER,
        default=0,
        label="Seed",
        info="Set to 0 to make random",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="self_attn_lr",
        widget=Widget.NUMBER,
        default=0.0,
        label="Self attn lr",
        info="Learning rate for self-attention layers. None=same as base LR, 0=freeze",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="show_timesteps",
        widget=Widget.DROPDOWN,
        default="",
        label="Show timesteps",
        info="Visualizes sampled timestep distribution and loss weighting, then exits without training. 'console'=ASCII histogram, 'image'=matplotlib plot. FLUX/SD3 only.",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["", "console", "image"],
    ),
    FieldSpec(
        name="show_timesteps_resolution",
        widget=Widget.TEXTBOX,
        default="1024",
        label="Show timesteps resolution",
        info="Image resolution (pixels) assumed by 'Show timesteps' for resolution-dependent sampling (e.g. flux_shift). Comma-separated: a single value is used for both H and W, two values are H,W.",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="shuffle_caption",
        widget=Widget.CHECKBOX,
        default=False,
        label="Shuffle caption",
        info="shuffle separated caption",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="sigmoid_scale",
        widget=Widget.NUMBER,
        default=1.0,
        label="Sigmoid Scale",
        info='Scale factor for sigmoid timestep sampling (used when Timestep Sampling is "sigmoid", "shift" or "flux_shift")',
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="skip_cache_check",
        widget=Widget.CHECKBOX,
        default=False,
        label="Skip cache check",
        info="Skip cache check for faster training start",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="skip_image_resolution",
        widget=Widget.TEXTBOX,
        default="",
        label="Skip image resolution",
        info="images not larger than this resolution will be skipped ('size' or 'width,height')",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="skip_until_initial_step",
        widget=Widget.CHECKBOX,
        default=False,
        label="Skip until initial step",
        info="skip training until initial_step is reached",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="split_attn",
        widget=Widget.CHECKBOX,
        default=False,
        label="Split Attention",
        info="Split attention computation to reduce memory usage. Required when using Attention Mode xformers.",
        archs=frozenset(["anima", "hunyuan_image"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="split_mode",
        widget=Widget.CHECKBOX,
        default=False,
        label="Split Mode",
        info="Split mode for Flux1",
        archs=frozenset(["flux1"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="system_prompt",
        widget=Widget.TEXTBOX,
        default="",
        label="System Prompt",
        info="System prompt to add to the prompt",
        archs=frozenset(["lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="t5_dropout_rate",
        widget=Widget.NUMBER,
        default=0.0,
        label="T5 Dropout Rate",
        info="Dropout rate for T5-XXL encoder",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="t5_max_token_length",
        widget=Widget.NUMBER,
        default=512,
        label="T5 Max Token Length",
        info="Maximum token length for the T5 tokenizer",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="t5_tokenizer_path",
        widget=Widget.TEXTBOX,
        default="",
        label="T5 Tokenizer Path",
        info="If omitted, uses the bundled tokenizer at configs/t5_old/",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="t5xxl",
        widget=Widget.TEXTBOX,
        default="",
        label="T5-XXL Path",
        info="path to t5xxl (*.sft or *.safetensors), should be float16",
        archs=frozenset(["flux1", "sd3"]),
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="t5xxl_device",
        widget=Widget.TEXTBOX,
        default="",
        label="T5-XXL Device",
        info="[DOES NOT WORK] not supported yet. T5-XXL device. if not specified, use accelerator's device",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="t5xxl_dtype",
        widget=Widget.TEXTBOX,
        default="",
        label="T5-XXL Dtype",
        info="[DOES NOT WORK] not supported yet. T5-XXL dtype. if not specified, use default dtype (from mixed precision)",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="t5xxl_max_token_length",
        widget=Widget.NUMBER,
        default=0,
        label="T5-XXL Max Token Length",
        info="Max token length for T5-XXL",
        archs=frozenset(["flux1", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="text_encoder",
        widget=Widget.TEXTBOX,
        default="",
        label="Qwen2.5-VL Path",
        info="path to Qwen2.5-VL (*.sft or *.safetensors), should be bfloat16",
        archs=frozenset(["hunyuan_image"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="text_encoder_batch_size",
        widget=Widget.NUMBER,
        default=0,
        label="Text encoder batch size",
        info="text encoder batch size (default: None, use dataset's batch size)",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="text_encoder_cpu",
        widget=Widget.CHECKBOX,
        default=False,
        label="Text Encoder on CPU",
        info="Run the Qwen2.5-VL and byT5 text encoders on CPU to reduce VRAM usage",
        archs=frozenset(["hunyuan_image"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="text_encoder_lr",
        widget=Widget.TEXTBOX,
        default="",
        label="Text encoder learning rate",
        info="Set to 0 to not train the Text Encoder",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
    ),
    FieldSpec(
        name="timestep_sampling",
        widget=Widget.DROPDOWN,
        default="sigma",
        label="Timestep Sampling",
        info="Method to sample timesteps: sigma-based, uniform random, sigmoid of random normal, shift of sigmoid and FLUX.1 shifting.",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["sigma", "uniform", "sigmoid", "shift", "flux_shift"],
    ),
    FieldSpec(
        name="token_warmup_min",
        widget=Widget.NUMBER,
        default=1,
        label="Token warmup min",
        info="start learning at N tags (token means comma separated strinfloatgs)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="token_warmup_step",
        widget=Widget.NUMBER,
        default=0,
        label="Token warmup step",
        info="tag length reaches maximum on N steps (or N*max_train_steps if N<1)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="dataset",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="tokenizer_cache_dir",
        widget=Widget.TEXTBOX,
        default="",
        label="Tokenizer cache dir",
        info="directory for caching Tokenizer (for offline training)",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="torch_compile",
        widget=Widget.CHECKBOX,
        default=False,
        label="Legacy Torch Compile (accelerate dynamo)",
        info="Use the legacy accelerate-dynamo torch.compile path instead of the per-block one. Mutually exclusive with Per-block Torch Compile above.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="train_batch_size",
        widget=Widget.NUMBER,
        default=1,
        label="Train batch size",
        info="batch size for training",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="train_data_dir",
        widget=Widget.TEXTBOX,
        default="",
        label="Train data dir",
        info="directory for train images",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="train_inpainting",
        widget=Widget.CHECKBOX,
        default=False,
        label="Train inpainting",
        info="train an inpainting model",
        archs=frozenset(["sd15", "sd2", "sdxl"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="training_comment",
        widget=Widget.TEXTBOX,
        default="",
        label="Training comment",
        info="arbitrary comment string stored in metadata",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="training_shift",
        widget=Widget.NUMBER,
        default=1.0,
        label="Training shift",
        info="Discrete flow shift for training timestep distribution adjustment, applied in addition to the weighting scheme, default is 1.0. /、、1.0。",
        archs=frozenset(["sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="unet_lr",
        widget=Widget.NUMBER,
        default=0.0,
        label="U-Net learning rate",
        info="learning rate for U-Net",
        archs=None,
        training_types=frozenset({"lora"}),
        group="basic",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="unsloth_offload_checkpointing",
        widget=Widget.CHECKBOX,
        default=False,
        label="Unsloth Offload Checkpointing",
        info="Offload activations to CPU RAM using async non-blocking transfers (faster than CPU Offload Checkpointing). Cannot be combined with CPU Offload Checkpointing or Blocks to swap.",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="use_flash_attn",
        widget=Widget.CHECKBOX,
        default=False,
        label="Use Flash Attention",
        info="Requires flash-attn package",
        archs=frozenset(["lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="use_sage_attn",
        widget=Widget.CHECKBOX,
        default=False,
        label="Use Sage Attention",
        info="Use Sage Attention for the model",
        archs=frozenset(["lumina"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="v2",
        widget=Widget.CHECKBOX,
        default=False,
        label="v2",
        info="load Stable Diffusion v2.0 model",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="v_parameterization",
        widget=Widget.CHECKBOX,
        default=False,
        label="v_param",
        info="enable v-parameterization training",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="v_pred_like_loss",
        widget=Widget.NUMBER,
        default=0.0,
        label="V Pred like loss",
        info="Recommended value of 0.5 when used",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="vae",
        widget=Widget.TEXTBOX,
        default="",
        label="VAE (Optional: Path to checkpoint of vae for training)",
        info="path to checkpoint of vae to replace",
        archs=None,
        training_types=frozenset({"lora"}),
        group="model",
    ),
    FieldSpec(
        name="vae_batch_size",
        widget=Widget.NUMBER,
        default=1,
        label="VAE batch size",
        info="batch size for caching latents",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="vae_chunk_size",
        widget=Widget.NUMBER,
        default=0,
        label="VAE Chunk Size",
        info="Spatial chunk size for VAE encoding/decoding to reduce memory usage. Must be an even number. 0 means no chunking (default).",
        archs=frozenset(["anima", "hunyuan_image"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="vae_disable_cache",
        widget=Widget.CHECKBOX,
        default=False,
        label="VAE Disable Cache",
        info="Disable internal VAE caching mechanism to reduce memory usage (faster, but differs from official behavior)",
        archs=frozenset(["anima"]),
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="validate_every_n_epochs",
        widget=Widget.NUMBER,
        default=0,
        label="Validate every n epochs",
        info="Run validation dataset every N epochs. By default, validation will run every epoch if a validation dataset is available",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="validate_every_n_steps",
        widget=Widget.NUMBER,
        default=0,
        label="Validate every n steps",
        info="Run validation on validation dataset every N steps. By default, validation will only occur every epoch if a validation dataset is available",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="validation_seed",
        widget=Widget.NUMBER,
        default=0,
        label="Validation seed",
        info="Validation seed for shuffling validation dataset, training `--seed` used otherwise",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_int,
        from_toml=_to_int,
    ),
    FieldSpec(
        name="validation_split",
        widget=Widget.NUMBER,
        default=0.0,
        label="Validation split",
        info="Split for validation images out of the training dataset",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
        to_toml=_to_float,
        from_toml=_to_float,
    ),
    FieldSpec(
        name="wandb_api_key",
        widget=Widget.TEXTBOX,
        default="",
        label="WANDB API Key",
        info="Users can obtain and/or generate an api key in the their user settings on the website: https://wandb.ai/login",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="wandb_run_name",
        widget=Widget.TEXTBOX,
        default="",
        label="WANDB run name",
        info="The name of the specific wandb session",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="weighted_captions",
        widget=Widget.CHECKBOX,
        default=False,
        label="Weighted captions",
        info="Enable weighted captions in the standard style (token:1.3). No commas inside parens, or shuffle/dropout may break the decoder.",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="weighting_scheme",
        widget=Widget.DROPDOWN,
        default="uniform",
        label="Weighting Scheme",
        info="weighting scheme for timestep distribution. Default is uniform, uniform and none are the same behavior",
        archs=frozenset(["anima", "flux1", "hunyuan_image", "lumina", "sd3"]),
        training_types=frozenset({"lora"}),
        group="advanced",
        choices=["sigma_sqrt", "logit_normal", "mode", "cosmap", "none", "uniform"],
    ),
    FieldSpec(
        name="xformers",
        widget=Widget.CHECKBOX,
        default=False,
        label="CrossAttention",
        info="use xformers for CrossAttention",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
    FieldSpec(
        name="zero_terminal_snr",
        widget=Widget.CHECKBOX,
        default=False,
        label="Zero terminal snr",
        info="fix noise scheduler betas to enforce zero terminal SNR",
        archs=None,
        training_types=frozenset({"lora"}),
        group="advanced",
    ),
]
