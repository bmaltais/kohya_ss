# Out of scope

Records of **rejected enhancement** requests for this repository.

- One file per concept (not per issue).
- Used during triage so the same idea is not re-litigated from scratch.
- **Do not** record “already implemented” features here — only true rejections / wrong-product requests.

See the triage skill `OUT-OF-SCOPE.md` for the full format.
